= content_for(:title, t('admin.statuses.new'))
= simple_form_for [:admin, @status] do |f|
  /! Main content
  .content
    .container-fluid
      /! /.container-fluid
      /! /.content
      .row
        .col-md-8
          .card.card-yellow
            .card-header
              = t('admin.statuses.new')
            .card-body
              = render partial: 'form', locals: {f: f}
            .card-footer
              = link_to  admin_statuses_path, class: 'btn btn-danger'
                = t(:cancel)
              = f.submit t(:save), class: 'btn btn-success pull-right'
