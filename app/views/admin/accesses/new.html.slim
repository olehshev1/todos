= content_for(:title, t('admin.accesses.new'))
= simple_form_for [:admin, @access] do |f|
  /! Main content
  .content
    .container-fluid
      /! /.container-fluid
      /! /.content
      .row
        .col-md-8
          .card.card-yellow
            .card-header
              = t('admin.accesses.new')
            .card-body
              = render partial: 'form', locals: {f: f}
            .card-footer
              = link_to  admin_accesses_path, class: 'btn btn-danger'
                = t(:cancel)
              = f.submit t(:save), class: 'btn btn-success pull-right'
