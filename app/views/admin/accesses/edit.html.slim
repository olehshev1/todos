= content_for(:title, t('admin.accesses.edit'))
= simple_form_for [:admin, @access] do |f|
  /! Main content
  .content
    .container-fluid
      /! /.container-fluid
      /! /.content
      .row
        .col-md-8
          .card.card-yellow
            .card-header
              = t('admin.accesses.edit')
            .card-body
              / - if @access.image.attached?
              /   div style=("border: 1px solid #28A745; padding: 2px")
              /     = image_tag @access.image.variant(resize: '200'), alt: @access.name
              /   = t('admin.accesses.remove')
              /   |  #{link_to t('do it'), destroy_image_admin_access_path(@access), method: :delete}
              /   p
              / - else
              /   p style=("border: 1px solid #28A745; padding: 2px")
              /     = image_tag 'backend/default-image.jpg', size: 200, alt: 'Photo Image'
              = render partial: 'form', locals: {f: f}
            .card-footer
              = link_to  admin_accesses_path, class: 'btn btn-danger'
                = t(:cancel)
              = f.submit t(:save), class: 'btn btn-success pull-right'
